Customize favicon
rename typeOsc to type
cleanup unused dials, both in HTML template and in fucntions, not every store action/function assumes the same ammount and types of dials
fix done dot blinking not correct size
hide done dots in navbar based on available modules
solve 'AudioParam.setValueAtTime' bug
