const noSequence =
  // {
  // return {

  fill(range(0, 16), {
    selected: false,
    pitch: 0,
    volume: false,
    glide: false
  });

// }
// }

import range from "lodash/range";
import fill from "lodash/fill";

export default noSequence;
